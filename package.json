{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node artifacts/api-server/dist/index.mjs",
    "heroku-postbuild": "npm install -g pnpm@9 && pnpm install && pnpm --filter @workspace/api-server run build"
  },
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
